apiVersion: 1

providers:
  - name: "Grafana"
    folder: "Services"
    type: "file"
    options:
      path: "/etc/grafana/provisioning/dashboards"

  - name: "Vehicles"
    folder: "Vehicles"
    type: file
    options:
      path: "/etc/grafana/provisioning/dashboards/vehicles"
