apiVersion: 1

providers:
  - name: "Grafana"
    folder: "Services"
    type: "file"
    options:
      path: "/etc/grafana/provisioning/dashboards"

  - name: "Devices"
    folder: "Devices"
    type: file
    options:
      path: "/etc/grafana/provisioning/dashboards/devices"
